name: sampleWorkFlow
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  printJob:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        name: checkout repository
      - name: execute script
        run: cat startup.txt
